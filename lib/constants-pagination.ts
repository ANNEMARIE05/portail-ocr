/** Options proposées pour le nombre de lignes affichées par page (listes admin et espace utilisateur). */
export const OPTIONS_ELEMENTS_PAR_PAGE_DEFAUT = [10, 25, 50] as const

/** Tickets assistance (pagination client ou API). */
export const OPTIONS_ELEMENTS_PAR_PAGE_TICKETS = [5, 10, 15, 25] as const
